<div class="container">
    <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <a class="navbar-brand" href="{{ path('homepage') }}">Vlille Alert</a>
    </div>

    <div id="navbar" class="navbar-collapse collapse">
        {% if app.user %}
            <div class="navbar-right"><a href="{{ path('fos_user_security_logout') }}">Deconnexion</a></div>
        {% else %}
            <div class="navbar-right"><a href="{{ path('fos_user_security_login') }}">Connexion</a></div>
        {% endif %}
    </div><!--/.navbar-collapse -->
</div>